# Dependency audit — lab_34 — 2026-05-24

## Summary

- **Audited roots:** `lab_34/`
- **Package managers:** npm
- **Outdated:** 1 | **Vulnerable:** 3 (moderate, transitive) | **Deprecated:** 0

## Manifests reviewed

| Root | Manager | Manifest | Lockfile |
|------|---------|----------|----------|
| `lab_34/` | npm | `package.json` | `package-lock.json` |

## Findings

### Vulnerabilities (fix first)

| Package | Current | Advisory / severity | Recommended action |
|---------|---------|---------------------|----------------------|
| `uuid` (transitive) | &lt;11.1.1 | [GHSA-w5hq-g745-h8pq](https://github.com/advisories/GHSA-w5hq-g745-h8pq) — moderate; buffer bounds check in v3/v5/v6 | Pulled in by `@langchain/langgraph` → `@langchain/langgraph-checkpoint`. Wait for upstream patch or track LangChain releases; **do not** run `npm audit fix --force` (downgrades `@langchain/langgraph` to 0.0.12). |

### Deprecated

| Package | Current | Note | Replacement / EOL |
|---------|---------|------|-------------------|
| — | — | None detected in this scan | — |

### Upgrade recommendations

#### Safe (patch / low risk)

| Package | Current | Target | Notes |
|---------|---------|--------|-------|
| `@langchain/core` | ^1.1.17 | latest 1.x | Already satisfied at install; re-run `npm outdated` after lock refresh if needed. |
| `@langchain/openai` | ^1.2.3 | latest 1.x | Same as above. |
| `dotenv` | ^17.2.3 | latest 17.x | Patch/minor within range. |

#### Minor (review changelog)

| Package | Current | Target | Notes |
|---------|---------|--------|-------|
| — | — | — | No minor-only bumps flagged by `npm outdated` beyond neo4j-driver major. |

#### Breaking (plan migration)

| Package | Current | Target | Migration notes |
|---------|---------|--------|-----------------|
| `neo4j-driver` | 5.28.3 | 6.0.1 | Major release; review [Neo4j driver 6.x migration](https://neo4j.com/docs/api/javascript-driver/current/) for API/session changes before bumping `package.json`. |

## Suggested order of work

1. Monitor `@langchain/langgraph` / checkpoint packages for a release that bumps transitive `uuid` to ≥11.1.1; avoid `npm audit fix --force`.
2. Plan `neo4j-driver` 6.x upgrade in a branch with integration tests against `docker-compose` Neo4j.
3. Commit or refresh `package-lock.json` after intentional upgrades only.

## Commands run

```bash
cd lab_34 && npm outdated
cd lab_34 && npm audit
```

## Limitations / not checked

- Docker base images in `docker-compose.yml` not scanned.
- No `pip`, `cargo`, or other manifests under `lab_34/`.
- Audit fix path suggested by npm is **breaking**; not applied.
