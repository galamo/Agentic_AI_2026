# OpenAI (required for embeddings)
OPENAI_API_KEY=your-openai-api-key

# Optional: Postgres (defaults shown; DB must be running on 5432)
# PG_HOST=127.0.0.1
# PG_PORT=5432
# PG_USER=langchain
# PG_PASSWORD=langchain
# PG_DATABASE=langchain

# Optional: Path to folder of TXT files to index (default: ./data)
# DATA_PATH=./data
