# Copy to .env and adjust if needed (docker-compose reads this file automatically when present).
# Used for credential encryption at rest — change for anything beyond local demos.

N8N_ENCRYPTION_KEY=lab25_change_me_to_a_long_random_string_at_least_32_chars
