OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Mermaid MCP server (Streamable HTTP)
MCP_MERMAID_URL=https://mcp.mermaid.ai/mcp
# Value to send as the HTTP `Authorization` header (see repo `mcp.json`).
MCP_MERMAID_AUTH=
